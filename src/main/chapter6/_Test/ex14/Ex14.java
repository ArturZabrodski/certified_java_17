package main.chapter6._Test.ex14;

/**
 * Which of the following statements about inheritance are correct? (Choose all that apply.)
 * A. A class can directly extend any number of classes.
 * +++B. A class can implement any number of interfaces.
 * C. All variables inherit java.lang.Object.
 * D. If class A is extended by B, then B is a superclass of A.
 * +++E. If class C implements interface D, then C is a subtype of D.
 * +++F. Multiple inheritance is the property of a class to have multiple direct superclasses.
 *
 * Какие из следующих утверждений о наследовании верны? (Выберите все, что применимо.)
 * A. Класс может напрямую расширять любое количество классов.
 * +++B. Класс может реализовывать любое количество интерфейсов.
 * C. Все переменные наследуют java.lang.Объект.
 * D. Если класс A расширен на B, то B является суперклассом A.
 * +++E. Если класс C реализует интерфейс D, то C является подтипом D.
 * +++F. Множественное наследование - это свойство класса иметь несколько прямых суперклассов.
 *
 * B, E, F. Класс может непосредственно расширять только один класс, что делает вариант A неправильным.
 * Однако класс может реализовать любое количество интерфейсов, что делает вариант B правильным.
 * Вариант C некорректен, поскольку типы примитивных переменных не наследуют java.lang.Объект.
 * Если класс расширяет другой класс, то это подкласс, а не суперкласс, что делает вариант D неправильным.
 * Класс, который реализует интерфейс, является подтипом этого интерфейса, что делает вариант E правильным.
 * Наконец, вариант F является правильным, поскольку это точное описание множественного наследования,
 * которое не допускается в Java.
 */

public class Ex14 {
}
